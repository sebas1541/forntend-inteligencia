# Copy to .env and fill in. Required for Google Maps (iOS + Android).
# Get a key at https://console.cloud.google.com/ (Maps SDK for iOS + Android),
# then restrict it to the bundle id / package: com.sebas1541.quickplate
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
